[pytest]
testpaths = .
python_files = test_*.py
asyncio_mode = auto
addopts = -m "not network" -p no:cacheprovider --import-mode=importlib
markers =
    network: tests that require live internet access (deselected by default; run with `-m network`)
filterwarnings =
    ignore::DeprecationWarning
